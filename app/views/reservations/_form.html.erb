<%= simple_form_for [bicycle, reservation] do |f| %>
  <%= f.input :start_date, as: :date, html5: true %>
  <%= f.input :end_date, as: :date, html5: true %>
  <%= f.button :submit, input_html: { class: "btn btn-primary" } %>
<% end %>
